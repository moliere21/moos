%include "sysconfig.inc"
;=================================================
;变量 : 保存 存储光标位置 的地址
;=================================================
global _Low_GetGdtPtr
extern gdtPtr
;=================================================
;加载 GDT
;=================================================
_Low_GetGdtPtr:      
    sgdt [gdtPtr]
    ret

;=================================================
;变量 : 保存 存储光标位置 的地址
;=================================================
global _Low_GetIdtPtr
extern idtPtr
;=================================================
;加载 Idt 
;=================================================
_Low_GetIdtPtr:
    sidt [idtPtr]
    ret